#!/data/data/com.termux/files/usr/bin/sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)"
PYTHON_BIN="${PYTHON_BIN:-python}"

PYTHONDONTWRITEBYTECODE=1 "$PYTHON_BIN" "$ROOT/src/api_cell/app.py" "$@"
