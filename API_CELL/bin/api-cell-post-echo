#!/data/data/com.termux/files/usr/bin/sh
set -eu

HOST="${API_CELL_HOST:-127.0.0.1}"
PORT="${API_CELL_PORT:-8765}"
MSG="${1:-hello}"

curl -sS -X POST "http://${HOST}:${PORT}/echo" \
  -H "Content-Type: application/json" \
  --data "{\"message\":\"$MSG\"}"
echo
